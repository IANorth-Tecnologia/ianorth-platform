# docker-compose.dev.yml (Attempt 4: Explicit Platform)
services:
  worker:
    platform: linux/arm64 # Explicitly target ARM for Mac development
    build:
      context: ./ianorth-api
      dockerfile: Dockerfile.worker.dev
    deploy:
      resources:
        reservations: {}
